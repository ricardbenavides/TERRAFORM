
locals {
  tags_default = {
    pais          = var.pais
    cliente       = var.cliente
    ambiente      = var.ambiente
    area          = var.area
    owner         = var.owner
    clasificacion = var.clasificacion
  }

}
